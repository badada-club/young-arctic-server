type Raffle {
    id: ID!
    name: String!
}

type RaffleUserInput {
    userId: ID!
    raffleId: ID!
}

type IsRaffle {
    raffle: Boolean!
    raffleId: Int
}

type Query {
    getRaffleId(eventId: Int): IsRaffle
}
type Mutation {
    takePart(userId: Int, raffleId: Int): String
}